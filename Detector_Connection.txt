Connect the MPU 6050 accelerometer sensor to the ESP8266 board using the I2C interface:
Connect the VCC pin of the MPU 6050 to the 3.3V pin of the ESP8266 board.
Connect the GND pin of the MPU 6050 to the GND pin of the ESP8266 board.
Connect the SDA pin of the MPU 6050 to the GPIO4(D2) pin of the ESP8266 board.
Connect the SCL pin of the MPU 6050 to the GPIO5(D1) pin of the ESP8266 board.
Connect the GPS module to the ESP8266 board using the serial communication interface:
Connect the VCC pin of the GPS module to the 3.3V pin of the ESP8266 board.
Connect the GND pin of the GPS module to the GND pin of the ESP8266 board.
Connect the TX pin of the GPS module to the GPIO15(D8) pin of the ESP8266 board.
Connect the RX pin of the GPS module to the GPIO13(D7) pin of the ESP8266 board.
Connect the ultrasonic sensor to the ESP8266 board using the digital input/output pins:
Connect the VCC pin of the ultrasonic sensor to the 5V pin of the ESP8266 board.
Connect the GND pin of the ultrasonic sensor to the GND pin of the ESP8266 board.
Connect the Echo pin of the ultrasonic sensor to the GPIO14(D5) pin of the ESP8266 board.
Connect the Trigger pin of the ultrasonic sensor to the GPIO12(D6) pin of the ESP8266 board.
Write a program to read data from the accelerometer sensor, GPS module, and ultrasonic sensor:
Use the MPU6050 library to read data from the accelerometer sensor.
Use the TinyGPS++ library to read data from the GPS module.
Use the NewPing library to read data from the ultrasonic sensor.
Combine the data from the accelerometer sensor, GPS module, and ultrasonic sensor and create a JSON object with the data:
Create a JSON object with keys for each of the data values (e.g., "latitude", "longitude", "acceleration_x", "acceleration_y", "acceleration_z", "distance").
Set the values of the JSON object to the

or 

Ultrasonic sensor:

Connect the VCC pin on the ultrasonic sensor to a 5V power source (such as a USB power bank or a separate power supply).
Connect the GND pin on the ultrasonic sensor to the GND pin on the ESP8266 module.
Connect the TRIG pin on the ultrasonic sensor to a GPIO output pin on the ESP8266 module.
Connect the ECHO pin on the ultrasonic sensor to a GPIO input pin on the ESP8266 module.
MPU6050 accelerometer sensor:

Connect the VCC pin on the MPU6050 to a 3.3V power source on the ESP8266 module.
Connect the GND pin on the MPU6050 to the GND pin on the ESP8266 module.
Connect the SDA pin on the MPU6050 to the SDA pin on the ESP8266 module (usually GPIO4-D2).
Connect the SCL pin on the MPU6050 to the SCL pin on the ESP8266 module (usually GPIO5-D1).
TCRT5000 IR sensor:

Connect the VCC pin on the TCRT5000 to a 5V power source (such as a USB power bank or a separate power supply).
Connect the GND pin on the TCRT5000 to the GND pin on the ESP8266 module.
Connect the OUT pin on the TCRT5000 to a GPIO input pin on the ESP8266 module.
Active buzzer:

Connect the VCC pin on the active buzzer to a 5V power source (such as a USB power bank or a separate power supply).
Connect the GND pin on the active buzzer to the GND pin on the ESP8266 module.
Connect the signal pin on the active buzzer to a GPIO output pin on the ESP8266 module.
Green and red lights:

Connect the VCC pin on each light to a 5V power source (such as a USB power bank or a separate power supply).
Connect the GND pin on each light to the GND pin on the ESP8266 module.
Connect the signal pin on the green light to a GPIO output pin on the ESP8266 module.
Connect the signal pin on the red light to a GPIO output pin on the ESP8266 module.